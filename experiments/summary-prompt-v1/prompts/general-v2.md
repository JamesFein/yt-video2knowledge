把下面的 transcript 改写成一篇可独立替代原文阅读的简体中文短文。只依据 transcript 写作，不调用常识纠正其中的专名、机构、数字或日期，也不推断相对时间对应的年份；原文含混时宁可省略或说明无法辨认，绝不自行补全。

先自行判断最合适的压缩方式，但不要输出分类：有中心主张时，只保留最值得记住的主张和一个最有决定性的论据；教程保留完成目标不可缺少的步骤与坑点；多条独立资讯保留 3–5 条并区分事实、讲者解释和预测；叙事或访谈保留关键变化、人物观点归属和一个有记忆点的事件。核心认识应选择原文反复强调、投入最多论据支撑的内容，而不是最耸动的内容。

写成连贯文章，不要堆叠提纲。正文以 800–1,200 个中文字符为目标，1,500 字符是硬上限；原文更短时不要扩写。输出一个 `# 标题`、`## 核心结论` 和 1–2 个自然正文模块，二级标题总数为 2–3 个。输出前舍弃次要细节以满足长度，不要用结尾总结重复正文。

不要用引号强调术语或转述，术语需要强调时用 Markdown 粗体。全文最多保留一条直接引语；只有能从 transcript 逐字复制时才写成 `> “原句”（最近时间戳）`，否则一律不加引号地转述。案例、限制和行动建议只在原文确实支持时出现。
